/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['dilfoods.in', 'luna-meraki.svc.urbanpiper.com', 'cdn.urbanpiper.com', 'your-image-url.com', 'ucarecdn.com', 't3.ftcdn.net'],
      },    
}

module.exports = nextConfig
